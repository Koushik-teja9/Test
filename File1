hi
sudo su - for authentication failed
inito    to close the machine
rpm -qa   it shows what are the packages installed
rpm -qa | grep httpd  for filteration of particular package
rpm -ivh    like to download particular package
rpm -ivh  i means installation
          v means verbos
          h means hashes
rpm -e    to earse the installed package
scp      move file from one server to another server
